#include <iostream>
#include <cstring>
using namespace std;

rep(char* s, char* s1, char* s2)
{
    int len_sub = strlen(s1);
    //将s1中的字符都用s2 中的字符替换 其中s1和s2的字符串的长度是相等的
    for(int j = 0; j < strlen(s1); ++j)
    {
         for(int i = 0; i < strlen(s); ++i)
         {
             if(s[i] == s[j])
             {
                 s[i]
             }

         }
    }


}


int main()
{

}


